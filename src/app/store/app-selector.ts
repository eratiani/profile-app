// import { createFeatureSelector, createSelector } from "@ngrx/store";
// import { AppInterface, UserInterface } from "./app.interface";

// export interface AppState {
//     auth:AppInterface
//     users:UserInterface[]
// }
// export const selectAuthState = createFeatureSelector<AppState>('app');

// // Selector to get the current user (auth)
// export const selectUser = createSelector(
//   selectAuthState,
//   (state: AppState) => state
// );



// // Selector to get users
// export const selectUsers = (state: AppState) => state.users;
// // Selector to get the authentication status
